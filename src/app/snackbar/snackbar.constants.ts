const ATTR = {
	live: 'aria-live',
	message: 'data-snackbar-message',
};

const CLASS = {
	active: 'mdf-snackbar--active',
};

const SELECTOR = {
	container: '.mdf-snackbar',
	text: '.mdf-snackbar__text',
};

export { ATTR, CLASS, SELECTOR };
