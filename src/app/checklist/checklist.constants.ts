const CLASS = {
	buttonInactive: 'mdf-button--hidden',
};

const SELECTOR = {
	input: '.mdf-checklist-textfield .mdf-textfield__input',
	saveButton: 'app-add > .mdf-slide__footer > .mdf-button',
	titleInput: '#checklist-title',
};

export { CLASS, SELECTOR };
